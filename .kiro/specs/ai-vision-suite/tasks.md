# AI Vision Suite — Tasks

Backfilled record of already-completed implementation work, not a forward plan.

## Core engine (Ollama plumbing)

- [x] Implement Ollama HTTP client: `isOllamaAvailable()`, `listOllamaModels()`, `generateCaption()` against `/api/tags` and `/api/generate` (`src/engine/caption/ollama.ts`) — Requirement 2
- [x] Define per-kind prompt templates (`alt`/`title`/`description`/`classify`/`tags`) with optional `--language` injection via `buildPrompt()` — Requirements 1, 4, 5, 6, 7, 10
- [x] Add pre-send image normalization: downscale to ≤1024px long edge, convert non-PNG/JPEG formats to PNG via lazy-loaded `sharp`, with graceful fallback to original bytes on failure (`downscaleForVision()`) — Requirement 12
- [x] Enforce a 200-token output cap (`num_predict`) and a 120s request timeout with a clear timeout error message — Requirement 12
- [x] Implement response cleanup per field kind: `cleanCaption()` (alt/description), `cleanTitle()`, `cleanClassify()` (earliest-label-match, negation-safe), `cleanTags()`/`cleanTagsArray()` — Requirement 13
- [x] Implement the garbage-output heuristic `looksLikeGarbage()` (short output, coordinate/float arrays, mostly-numeric output) — Requirement 11
- [x] Implement `generateCaptionWithFallback()`: retry against a configured fallback model when the primary result looks like garbage, and pick the longer of two garbage results if the fallback also fails the heuristic — Requirement 11
- [x] Define shared types `VisionKind`, `CaptionOptions`, `CaptionResult` (`src/engine/caption/types.ts`)
- [x] Implement shared bulk-run helper `preflightOllama()` (availability + installed-model check with remediation text) and `runBulkVision()` (shared per-item loop: upsert-first, skip-if-set, fetch, generate, snapshot, write, record) in `src/engine/caption/run-bulk.ts` — Requirement 2, 3

## CLI command wiring

- [x] `caption` command: explicit-ID and `--missing-alt`/`--all` bulk modes, `--overwrite`, `--language`, `--fallback-model`, `--list-models`, `--ollama-url`, `--model`, `--prompt` (`src/cli/commands/caption.ts`) — Requirements 1, 3, 9, 10, 11
- [x] `title` command built on `runBulkVision()`: `--missing-title` auto-generated-title detection (`looksAutoGenerated()`), `--all`, `--overwrite`, `--language` (`src/cli/commands/title.ts`) — Requirements 3, 4, 10
- [x] `describe` command built on `runBulkVision()`: `--missing-description`, `--all`, `--overwrite`, `--language` (`src/cli/commands/describe.ts`) — Requirements 3, 5, 10
- [x] `classify` command: explicit-IDs-only, closed-vocabulary classification, local SQLite cache via `processing_history`, no WordPress write, `getCachedClassification()` export consumed by `optimize` (`src/cli/commands/classify.ts`) — Requirement 6
- [x] `tag` command: `[tags: …]` block read/write against the WP `caption` field, `--missing-tags`, `--all`, `--overwrite` (block-replace vs. append semantics) (`src/cli/commands/tag.ts`) — Requirements 3, 7
- [x] `vision` composed command: `--fields` subsetting/validation, print-only-by-default posture distinct from bulk dry-run convention, single combined `updateMetadata()` write per item, classification caching on apply (`src/cli/commands/vision.ts`) — Requirement 8
- [x] Wire dry-run/apply semantics for all bulk-capable commands through the `resolveDryRun()` helper (or the equivalent explicit `parentOpts.apply` check in `vision`) so explicit IDs execute immediately and bulk flags default to preview (`src/cli/utils/run-mode.ts`) — Requirement 1, 3, 8
- [x] Wire time-machine snapshot capture (metadata-only: altText/title/caption/description) before each non-dry-run mutation in `caption`, `title`/`describe` (via `run-bulk.ts`), `tag`, and `vision --apply`, using `openHistorySession`/`captureSnapshot`/`closeHistorySession` — Requirement 1 (snapshot criterion), design "Time-machine integration"
- [x] FK-safe processing-history bookkeeping: unconditional `upsertAttachment()` before any network call, best-effort try/catch around failure-row `recordProcessing()` calls so a bookkeeping error never aborts the bulk loop — Error handling in design.md

## MCP tool wiring

- [x] `caption` MCP tool, including ID-count batching via `runCliBatched()` to avoid per-call timeouts on large bulk requests (`src/cli/mcp/tools.ts`)
- [x] `generate_title` MCP tool (maps to the `title` CLI command)
- [x] `generate_description` MCP tool (maps to the `describe` CLI command)
- [x] `vision` MCP tool (unified fields subset + apply)
- [x] `tag` MCP tool
- [x] `classify` MCP tool

## Tests

- [x] Unit tests for `cleanCaption()` covering quote stripping, meta-phrase intro removal, multi-paragraph/bullet trimming, word-boundary truncation, and a real verbose-model regression case (`test/unit/caption-clean.test.ts`)
- [x] Unit tests for `cleanClassify()` covering plain-label match, negation avoidance, earliest-match-wins, and fallback-to-first-word (`test/unit/caption-clean.test.ts`)
- [x] Static dry-run-honesty check asserting `caption.ts`/`title.ts`/`describe.ts`/`tag.ts`/`vision.ts` each contain a recognized dry-run gate (`test/unit/dry-run-honesty.test.ts`)
- [x] Command-tree wiring test asserting no vision-suite command locally redeclares the global `--dry-run`/`--apply` flags (`test/unit/dry-run-wiring.test.ts`)
- [x] MCP schema presence and CLI-flag parity checks covering the `caption` tool (`test/unit/mcp.test.ts`, `test/unit/mcp-schema-cli-parity.test.ts`)

## Documentation

- [x] README: alt-text generation section (Ollama setup, `caption --missing-alt --apply`, `--language`, no-cloud/no-credits framing) and command tables listing `title`/`describe`/`classify`/`tag`/`vision` under "AI Vision"
- [x] Skill reference (`skill/SKILL.md`) documents the vision-suite commands and JSON schemas for agent consumption (per CLAUDE.md's repo map; not re-verified line-by-line as part of this backfill — flagged for a follow-up pass if the skill doc needs updating to match current flags)
- [x] CLAUDE.md implementation-status entry summarizing the AI vision suite under "AI enrichment (local Ollama vision, no cloud)"
