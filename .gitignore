# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
*.tsbuildinfo

.kiro
CLAUDE.md
docs

# Bun
bun.lockb

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/
*.lcov
.nyc_output/

# Local development scratch space
scratch/
*.local

# Homebrew tap repo — checked out locally for reference/editing,
# but it's a separate git repo (gfargo/homebrew-localpress).
# Same pattern as .wiki for project wikis.
.tap/

# Marketing website repo — checked out locally for reference/editing,
# separate git repo (gfargo/localpress-www).
.www/

.wiki/

# Models (downloaded at runtime, not committed)
models/
*.onnx

# Screenshot pipeline raw output (regenerated, not committed)
bin/screenshot/out/
bin/screenshot/tapes/
!bin/screenshot/out/.gitkeep

# Local config (per-developer test sites)
.localpress/
